# AR 3D Face Calibration Next Steps - 2026-05-07 14:16

## สถานะล่าสุด

จากภาพล่าสุด:

- หันซ้าย/หันขวา หรือ yaw ใกล้เป๊ะแล้ว
- เส้น debug 2D ยังเกาะใบหน้าถูก
- แว่นหมุนตามเอียงคอได้ดีกว่าโมเดล 3D
- 3D face model ยังอยู่ต่ำและขวากว่าหน้าจริง
- ตอนเอียงคอ roll ยังไม่ตามใบหน้าเท่าที่ควร
- log ปัจจุบัน:
  - `source=1280x720`
  - `render=860x860`
  - `mode=CanonicalMatrix`
  - `localPosition=(0.00, 0.00, 0.00)`
  - `localEuler=(0.00, 180.00, 0.00)`
  - `localScale=(-10.00, 10.00, 10.00)`

ข้อสรุป: projection และ yaw ไม่ใช่ตัวหลักแล้ว งานที่เหลือคือ calibration ของตำแหน่ง local offset และ roll/pitch coupling ของโมเดล

## ห้ามแก้ก่อน

- อย่าเปลี่ยน `Invert Face Yaw`
- อย่าเปลี่ยนแกน Y ของ `Euler Offset` ถ้า yaw ซ้าย/ขวาเป๊ะแล้ว
- อย่าแก้ projection mapping เพิ่ม
- อย่าเพิ่ม scale ก่อนตำแหน่ง X/Y เข้าใกล้หน้า

## ลำดับแก้ที่แนะนำ

### 1. แก้ตำแหน่งก่อน

จากภาพ โมเดลต้องเลื่อนกลับ:

- ไปซ้าย
- ขึ้นบน

ใน Inspector ให้ปรับ `Tracked 3D Face Guide Local Offset` ก่อน โดยใช้ค่าประมาณเริ่มต้น:

```text
X = -0.20 ถึง -0.45
Y =  0.20 ถึง  0.45
Z =  0.00
```

วิธีเลือกค่า:

- ถ้าโมเดลยังอยู่ขวา ให้ลด X เพิ่ม
- ถ้าโมเดลยังต่ำ ให้เพิ่ม Y
- ถ้าโมเดลเริ่มตรงกลางหน้าแล้ว ให้หยุดแก้ X/Y และไปขั้นต่อไป

ให้เทสต์หน้าตรงก่อนเท่านั้น อย่าเพิ่งเทสต์เอียงคอจนกว่า position จะเข้าที่

### 2. แก้ scale หลังตำแหน่งเข้าที่

ตอนนี้ scale log เป็นประมาณ `3.441` และ localScale เป็น `10` ทำให้ขนาดดูค่อนข้างใหญ่เมื่อเทียบกับตำแหน่งที่ยังหลุด

หลังเลื่อนตำแหน่งเข้าหน้าแล้ว ค่อยลอง:

```text
Tracked 3D Face Guide Scale: ลดทีละ 0.05 - 0.15
หรือ
Tracked 3D Face Guide Local Scale: ลดจาก 10 เป็น 8-9
```

เป้าคือโมเดลไม่ใหญ่กว่ากรอบใบหน้า debug มากเกินไป

### 3. แก้เอียงคอด้วย Roll ไม่ใช่ Yaw

เพราะ yaw เป๊ะแล้ว ปัญหาเอียงคอควรแก้ที่แกน Z หรือ roll correction

ใน `Euler Offset` ให้ลองแกน Z เท่านั้นก่อน:

```text
Z = -10, -5, 0, 5, 10
```

วิธีตัดสิน:

- เอียงคอซ้าย ถ้าโมเดลเอียงน้อยกว่าหน้าจริง ให้เพิ่มแรง roll ไปทิศเดียวกัน
- เอียงคอซ้าย ถ้าโมเดลสวนทาง ให้กลับเครื่องหมาย Z
- ห้ามเปลี่ยน Y ระหว่างเทสต์นี้ เพราะจะทำให้ yaw ที่เป๊ะอยู่เสีย

### 4. ถ้า roll ยังตามไม่ทัน

ถ้าปรับ `Euler Offset Z` แล้วช่วยเฉพาะมุมกลาง แต่ตอนเอียงมากยังไม่เป๊ะ แปลว่า fixed offset ไม่พอ ต้องให้ GLM เพิ่ม parameter ใหม่ในโค้ดภายหลัง:

```text
tracked3dFaceGuideRollMultiplier
default = 1.0
range = 0.5 - 1.5
```

แนวคิด:

- yaw ใช้ค่าปัจจุบันเหมือนเดิม
- pitch ใช้ค่าปัจจุบันเหมือนเดิม
- roll คูณ multiplier แยก เพื่อให้เอียงคอตามจริงมากขึ้นหรือน้อยลง

แต่ขั้นนี้ยังไม่ควรทำจนกว่าจะลอง offset/scale/Fixed Z offset แล้ว

## Preset ที่ควรลอง

ให้ลองทีละ preset และจดผล:

```text
Preset A
Local Offset = (-0.25, 0.25, 0.00)
Euler Offset = (0, 180, 0)
Local Scale  = (-10, 10, 10)
```

```text
Preset B
Local Offset = (-0.35, 0.30, 0.00)
Euler Offset = (0, 180, 0)
Local Scale  = (-10, 10, 10)
```

```text
Preset C
Local Offset = (-0.35, 0.30, 0.00)
Euler Offset = (0, 180, 5)
Local Scale  = (-9, 9, 9)
```

```text
Preset D
Local Offset = (-0.35, 0.35, 0.00)
Euler Offset = (0, 180, -5)
Local Scale  = (-9, 9, 9)
```

## วิธีเทสต์ให้ได้คำตอบเร็ว

1. หน้าตรง

   เช็กแค่ว่าโมเดลอยู่กลางใบหน้าและสูงพอหรือยัง

2. หันซ้าย/ขวาเล็กน้อย

   เช็กว่า yaw ยังไม่พังหลังปรับ offset

3. เอียงคอซ้าย/ขวา

   เช็กว่า roll ไปทางเดียวกับหน้าไหม และมาก/น้อยเกินไปไหม

4. Capture 1 รูป

   เช็กว่า live preview กับภาพ capture ตรงกัน

## เกณฑ์ผ่านรอบนี้

- หน้าตรง: โมเดลไม่หลุดขวาล่างแล้ว
- หันซ้าย/ขวา: ยังตามได้เหมือนเดิม
- เอียงคอ: โมเดลเอียงไปทิศเดียวกับหน้า แม้ยังไม่ต้องเป๊ะ 100%
- Capture: ตำแหน่งใกล้เคียง live preview

## ถ้ายังไม่ผ่าน

ถ้าทำ preset แล้วตำแหน่งดีขึ้นแต่ roll ยังไม่ดี ให้ขั้นถัดไปเป็นงาน code เฉพาะเรื่อง roll multiplier เท่านั้น ไม่ควรแก้ projection หรือ yaw อีก
